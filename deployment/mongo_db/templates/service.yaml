apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongo_db.fullname" . }}
  labels:
    {{- include "mongo_db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mongo_db.selectorLabels" . | nindent 4 }}
